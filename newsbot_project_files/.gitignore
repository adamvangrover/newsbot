# General
.DS_Store
*.log
*.tmp
*.swp
*~
*.bak

# Python / Backend
__pycache__/
*.py[cod]
*$py.class
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST

# Virtual environment
.python-version
.venv/
venv/
env/
ENV/
pip-wheel-metadata/
# If you use Conda
# .conda/
# conda-meta/

# Secrets - IMPORTANT: Ensure .env files are NOT committed
.env
*.env
.env.*
!.env.example # Allow .env.example files

# Pytest
.pytest_cache/
.coverage
.coverage.*
htmlcov/
nosetests.xml
coverage.xml
*.cover

# MyPy
.mypy_cache/

# Jupyter Notebook
.ipynb_checkpoints

# Frontend (Node.js / Vite / React)
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
# Vite specific
dist/ # Vite build output
# If CRA was used:
# build/ # CRA build output

# Editor-specific files
.vscode/
.idea/
*.sublime-project
*.sublime-workspace

# OS specific
Thumbs.db
ehthumbs.db
Desktop.ini

# Docker related (if needed, though .dockerignore is preferred for Docker context)
# Might want to ignore local Docker build caches or exported images if stored in project
# *.tar
# *.tar.gz

# Hugging Face cache (often global, but if redirected locally)
.cache/huggingface/
# However, this is usually better handled by user's global gitignore or HF_HOME settings
